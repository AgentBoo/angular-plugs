import { CompatibilityVerbosePipe } from './compatibility-verbose.pipe';

describe('CompatibilityVerbosePipe', () => {
  it('create an instance', () => {
    const pipe = new CompatibilityVerbosePipe();
    expect(pipe).toBeTruthy();
  });
});
